<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mall of America - Premium Online Shopping</title>
    <link rel="stylesheet" href="style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body>
    <!-- Navigation -->
    <nav class="navbar">
        <div class="nav-container">
            <div class="nav-brand">
                <i class="fas fa-shopping-bag"></i>
                <span>MALL OF AMERICA</span>
            </div>
            <button type="button" class="mobile-menu-btn" id="mobileMenuBtn">
                <i class="fas fa-bars"></i>
            </button>
            <ul class="nav-links" id="navLinks">
                <li><a href="#home" class="active">Home</a></li>
                <li><a href="#products">Products</a></li>
                <li><a href="#categories">Categories</a></li>
                <li><a href="#deals">Deals</a></li>
                <li><a href="#about">About</a></li>
                <li><a href="#contact">Contact</a></li>
            </ul>
            <div class="nav-actions">
                <button type="button" class="icon-btn" id="searchToggle">
                    <i class="fas fa-search"></i>
                </button>
                <button type="button" class="icon-btn cart-btn" id="cartToggle">
                    <i class="fas fa-shopping-cart"></i>
                    <span class="cart-count" id="cartCount">0</span>
                </button>
                <button type="button" class="icon-btn admin-btn" id="adminToggle">
                    <i class="fas fa-cog"></i>
                </button>
            </div>
        </div>
    </nav>

    <!-- Search Bar -->
    <div class="search-bar" id="searchBar">
        <div class="search-container">
            <input type="text" id="searchInput" placeholder="Search products by name or category...">
            <button type="button" id="searchBtn"><i class="fas fa-search"></i></button>
        </div>
    </div>

    <!-- Hero Section -->
    <section class="hero" id="home">
        <div class="hero-content">
            <h1>Welcome to Mall of America</h1>
            <p>Discover premium products at unbeatable prices</p>
            <a href="#products" class="btn btn-primary">Shop Now</a>
        </div>
    </section>

    <!-- Main Content -->
    <main class="main-content">
        <!-- Filters Section -->
        <section class="filters-section" id="categories">
            <div class="container">
                <div class="category-filters">
                    <button type="button" class="category-btn active" data-category="all">All</button>
                    <button type="button" class="category-btn" data-category="Electronics">Electronics</button>
                    <button type="button" class="category-btn" data-category="Fashion">Fashion</button>
                    <button type="button" class="category-btn" data-category="Beauty">Beauty</button>
                    <button type="button" class="category-btn" data-category="Home">Home</button>
                    <button type="button" class="category-btn" data-category="Gaming">Gaming</button>
                    <button type="button" class="category-btn" data-category="Accessories">Accessories</button>
                </div>
                <div class="sort-options">
                    <select id="sortSelect">
                        <option value="default">Sort By</option>
                        <option value="price-low">Price: Low to High</option>
                        <option value="price-high">Price: High to Low</option>
                        <option value="rating">Highest Rated</option>
                        <option value="newest">Newest Products</option>
                    </select>
                </div>
            </div>
        </section>

        <!-- Products Section -->
        <section class="products-section" id="products">
            <div class="container">
                <h2 class="section-title">Featured Products</h2>
                <div class="products-grid" id="productsGrid"></div>
            </div>
        </section>

        <!-- Deals Section -->
        <section class="deals-section" id="deals">
            <div class="container">
                <h2 class="section-title">Today's Deals</h2>
                <div class="deals-grid" id="dealsGrid"></div>
            </div>
        </section>

        <!-- About Section -->
        <section class="about-section" id="about">
            <div class="container">
                <div class="about-content">
                    <div class="about-text">
                        <h2>About Mall of America</h2>
                        <p>Your premier destination for online shopping. We curate the finest products from around the world, offering exceptional quality, competitive prices, and outstanding customer service.</p>
                    </div>
                </div>
            </div>
        </section>

        <!-- Contact Section -->
        <section class="contact-section" id="contact">
            <div class="container">
                <h2 class="section-title">Contact Us</h2>
                <div class="contact-content">
                    <div class="contact-info">
                        <p><i class="fas fa-envelope"></i> support@mallofamerica.com</p>
                        <p><i class="fas fa-phone"></i> 1-800-MALL-USA</p>
                        <p><i class="fas fa-map-marker-alt"></i> 60 E Broadway, Bloomington, MN</p>
                    </div>
                </div>
            </div>
        </section>
    </main>

    <!-- Footer -->
    <footer class="footer">
        <div class="container">
            <p>&copy; 2026 Mall of America. All rights reserved.</p>
        </div>
    </footer>

    <!-- Cart Side Panel -->
    <div class="cart-overlay" id="cartOverlay"></div>
    <div class="cart-panel" id="cartPanel">
        <div class="cart-header">
            <h3><i class="fas fa-shopping-cart"></i> Your Cart</h3>
            <button type="button" class="close-cart" id="closeCart"><i class="fas fa-times"></i></button>
        </div>
        <div class="cart-items" id="cartItems"></div>
        <div class="cart-footer">
            <div class="cart-subtotal">
                <span>Subtotal:</span>
                <span id="cartSubtotal">$0.00</span>
            </div>
            <div class="cart-total">
                <span>Total:</span>
                <span id="cartTotal">$0.00</span>
            </div>
            <button type="button" class="btn btn-primary btn-block" id="checkoutBtn">Checkout</button>
            <button type="button" class="btn btn-secondary btn-block" id="clearCartBtn">Clear Cart</button>
        </div>
    </div>

    <!-- Product Details Modal -->
    <div class="modal-overlay" id="productModalOverlay"></div>
    <div class="modal product-modal" id="productModal">
        <div class="modal-content">
            <button type="button" class="modal-close" id="closeProductModal"><i class="fas fa-times"></i></button>
            <div class="product-detail-grid" id="productDetailContent"></div>
        </div>
    </div>

    <!-- Admin Panel Modal -->
    <div class="modal-overlay" id="adminModalOverlay"></div>
    <div class="modal admin-modal" id="adminModal">
        <div class="modal-content">
            <div class="admin-header">
                <h2><i class="fas fa-cog"></i> Admin Dashboard</h2>
                <button type="button" class="modal-close" id="closeAdminModal"><i class="fas fa-times"></i></button>
            </div>
            <div class="admin-stats" id="adminStats"></div>
            <div class="admin-actions">
                <button type="button" class="btn btn-primary" id="addProductBtn"><i class="fas fa-plus"></i> Add Product</button>
            </div>
            <div class="admin-table-container">
                <table class="admin-table" id="adminTable">
                    <thead>
                        <tr>
                            <th>Product</th>
                            <th>Category</th>
                            <th>Price</th>
                            <th>Stock</th>
                            <th>Rating</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody id="adminTableBody"></tbody>
                </table>
            </div>
        </div>
    </div>

    <!-- Add/Edit Product Modal -->
    <div class="modal-overlay" id="productFormOverlay"></div>
    <div class="modal product-form-modal" id="productFormModal">
        <div class="modal-content">
            <h3 id="productFormTitle">Add Product</h3>
            <form id="productForm">
                <input type="hidden" id="editProductId">
                <div class="form-group">
                    <label>Product Name</label>
                    <input type="text" id="formProductName" required>
                </div>
                <div class="form-group">
                    <label>Category</label>
                    <select id="formProductCategory" required>
                        <option value="Electronics">Electronics</option>
                        <option value="Fashion">Fashion</option>
                        <option value="Beauty">Beauty</option>
                        <option value="Home">Home</option>
                        <option value="Gaming">Gaming</option>
                        <option value="Accessories">Accessories</option>
                    </select>
                </div>
                <div class="form-group">
                    <label>Description</label>
                    <textarea id="formProductDescription" rows="3" required></textarea>
                </div>
                <div class="form-group">
                    <label>Image URL</label>
                    <input type="url" id="formProductImage" required>
                </div>
                <div class="form-row">
                    <div class="form-group">
                        <label>Original Price</label>
                        <input type="number" id="formOriginalPrice" step="0.01" min="0" required>
                    </div>
                    <div class="form-group">
                        <label>Current Price</label>
                        <input type="number" id="formCurrentPrice" step="0.01" min="0" required>
                    </div>
                </div>
                <div class="form-row">
                    <div class="form-group">
                        <label>Stock Quantity</label>
                        <input type="number" id="formStockQuantity" min="0" required>
                    </div>
                    <div class="form-group">
                        <label>Initial Rating (0-5)</label>
                        <input type="number" id="formInitialRating" step="0.1" min="0" max="5" required>
                    </div>
                </div>
                <div class="form-group">
                    <label>Rating Count</label>
                    <input type="number" id="formRatingCount" min="0" required>
                </div>
                <div class="form-actions">
                    <button type="submit" class="btn btn-primary">Save Product</button>
                    <button type="button" class="btn btn-secondary" id="cancelProductForm">Cancel</button>
                </div>
            </form>
        </div>
    </div>

    <!-- Update Price Modal -->
    <div class="modal-overlay" id="priceModalOverlay"></div>
    <div class="modal price-modal" id="priceModal">
        <div class="modal-content">
            <h3>Update Price</h3>
            <form id="priceForm">
                <input type="hidden" id="priceProductId">
                <div class="form-group">
                    <label>Original Price</label>
                    <input type="number" id="updateOriginalPrice" step="0.01" min="0" required>
                </div>
                <div class="form-group">
                    <label>Current Price</label>
                    <input type="number" id="updateCurrentPrice" step="0.01" min="0" required>
                </div>
                <div class="form-group">
                    <label>Discount %</label>
                    <input type="number" id="updateDiscount" step="1" min="0" max="100" required>
                </div>
                <div class="form-actions">
                    <button type="submit" class="btn btn-primary">Update Price</button>
                    <button type="button" class="btn btn-secondary" id="cancelPriceUpdate">Cancel</button>
                </div>
            </form>
        </div>
    </div>

    <!-- Update Rating Modal -->
    <div class="modal-overlay" id="ratingModalOverlay"></div>
    <div class="modal rating-modal" id="ratingModal">
        <div class="modal-content">
            <h3>Update Rating</h3>
            <form id="ratingForm">
                <input type="hidden" id="ratingProductId">
                <div class="form-group">
                    <label>Rating (0-5)</label>
                    <input type="number" id="updateRatingValue" step="0.1" min="0" max="5" required>
                </div>
                <div class="form-group">
                    <label>Rating Count</label>
                    <input type="number" id="updateRatingCount" min="0" required>
                </div>
                <div class="form-actions">
                    <button type="submit" class="btn btn-primary">Update Rating</button>
                    <button type="button" class="btn btn-secondary" id="cancelRatingUpdate">Cancel</button>
                </div>
            </form>
        </div>
    </div>

    <!-- Toast Container -->
    <div class="toast-container" id="toastContainer"></div>

    <script src="script.js"></script>
</body>
</html>
/**
 * MALL OF AMERICA - Complete Shopping Mall Website
 * Pure HTML5, CSS3, Vanilla JavaScript
 * No frameworks, no backend
 * 
 * NOTE: This is a frontend demo. Real authentication requires a backend server.
 */

// ==========================================
// STATE MANAGEMENT
// ==========================================

let products = [];
let cart = [];
let currentCategory = 'all';
let currentSort = 'default';
let searchQuery = '';

// ==========================================
// DEFAULT PRODUCTS
// ==========================================

const defaultProducts = [
    {
        id: '1',
        name: 'iPhone 17 Pro',
        category: 'Electronics',
        description: 'The most advanced iPhone ever with A19 Pro chip, titanium design, and revolutionary camera system.',
        image: 'https://images.unsplash.com/photo-1511707171634-5f897ff02aa9?w=400',
        originalPrice: 1299.00,
        currentPrice: 1199.00,
        discount: 8,
        rating: 4.8,
        ratingCount: 125,
        stock: 45,
        createdAt: '2026-01-15'
    },
    {
        id: '2',
        name: 'Samsung Galaxy S26',
        category: 'Electronics',
        description: 'Experience the future with Galaxy AI, 200MP camera, and all-day battery life.',
        image: 'https://images.unsplash.com/photo-1610945415295-d9bbf067e59c?w=400',
        originalPrice: 1199.00,
        currentPrice: 1099.00,
        discount: 8,
        rating: 4.7,
        ratingCount: 98,
        stock: 32,
        createdAt: '2026-02-01'
    },
    {
        id: '3',
        name: 'MacBook Air M4',
        category: 'Electronics',
        description: 'Supercharged by M4 chip with up to 18 hours of battery life in a stunningly thin design.',
        image: 'https://images.unsplash.com/photo-1517336714731-489689fd1ca4?w=400',
        originalPrice: 1499.00,
        currentPrice: 1399.00,
        discount: 7,
        rating: 4.9,
        ratingCount: 210,
        stock: 28,
        createdAt: '2026-01-20'
    },
    {
        id: '4',
        name: 'Nike Air Max 2026',
        category: 'Fashion',
        description: 'Revolutionary cushioning technology meets iconic style. The future of comfort is here.',
        image: 'https://images.unsplash.com/photo-1542291026-7eec264c27ff?w=400',
        originalPrice: 189.99,
        currentPrice: 149.99,
        discount: 21,
        rating: 4.6,
        ratingCount: 342,
        stock: 67,
        createdAt: '2026-03-01'
    },
    {
        id: '5',
        name: 'Adidas Ultraboost X',
        category: 'Fashion',
        description: 'Energy return meets style. The most responsive cushioning ever in a sleek package.',
        image: 'https://images.unsplash.com/photo-1587563871167-1ee9c731aefb?w=400',
        originalPrice: 179.99,
        currentPrice: 139.99,
        discount: 22,
        rating: 4.5,
        ratingCount: 189,
        stock: 54,
        createdAt: '2026-02-15'
    },
    {
        id: '6',
        name: 'Sony WH-1000XM6',
        category: 'Accessories',
        description: 'Industry-leading noise cancellation with premium sound quality and 30-hour battery life.',
        image: 'https://images.unsplash.com/photo-1505740420928-5e560c06d30e?w=400',
        originalPrice: 399.99,
        currentPrice: 349.99,
        discount: 13,
        rating: 4.8,
        ratingCount: 567,
        stock: 23,
        createdAt: '2026-01-10'
    },
    {
        id: '7',
        name: 'Apple Watch Ultra 3',
        category: 'Accessories',
        description: 'The most rugged and capable Apple Watch with advanced health and fitness features.',
        image: 'https://images.unsplash.com/photo-1523275335684-37898b6baf30?w=400',
        originalPrice: 899.00,
        currentPrice: 849.00,
        discount: 6,
        rating: 4.7,
        ratingCount: 234,
        stock: 19,
        createdAt: '2026-03-10'
    },
    {
        id: '8',
        name: 'ROG Strix Gaming Laptop',
        category: 'Gaming',
        description: 'Dominate the game with RTX 5090, Intel Core Ultra 9, and 240Hz OLED display.',
        image: 'https://images.unsplash.com/photo-1603302576837-37561b2e2302?w=400',
        originalPrice: 2499.00,
        currentPrice: 2199.00,
        discount: 12,
        rating: 4.9,
        ratingCount: 156,
        stock: 12,
        createdAt: '2026-02-20'
    },
    {
        id: '9',
        name: 'Samsung 75" QN900D 8K TV',
        category: 'Home',
        description: 'The ultimate viewing experience with 8K resolution, AI upscaling, and Neo QLED technology.',
        image: 'https://images.unsplash.com/photo-1593359677879-a4bb92f829d1?w=400',
        originalPrice: 4999.00,
        currentPrice: 4499.00,
        discount: 10,
        rating: 4.8,
        ratingCount: 89,
        stock: 8,
        createdAt: '2026-01-05'
    },
    {
        id: '10',
        name: 'Logitech MX Mechanical',
        category: 'Accessories',
        description: 'Precision mechanical keyboard with smart illumination and multi-device connectivity.',
        image: 'https://images.unsplash.com/photo-1587829741301-dc798b83add3?w=400',
        originalPrice: 169.99,
        currentPrice: 149.99,
        discount: 12,
        rating: 4.6,
        ratingCount: 445,
        stock: 78,
        createdAt: '2026-03-05'
    }
];

// ==========================================
// LOCAL STORAGE FUNCTIONS
// ==========================================

function loadFromStorage(key, defaultValue) {
    try {
        const data = localStorage.getItem(key);
        return data ? JSON.parse(data) : defaultValue;
    } catch (e) {
        console.error('Error loading from localStorage:', e);
        return defaultValue;
    }
}

function saveToStorage(key, value) {
    try {
        localStorage.setItem(key, JSON.stringify(value));
    } catch (e) {
        console.error('Error saving to localStorage:', e);
    }
}

// ==========================================
// INITIALIZATION
// ==========================================

function init() {
    // Load products from localStorage or use defaults
    products = loadFromStorage('moa_products', null);
    if (!products || products.length === 0) {
        products = [...defaultProducts];
        saveToStorage('moa_products', products);
    }
    
    // Load cart from localStorage
    cart = loadFromStorage('moa_cart', []);
    
    // Render all sections
    renderProducts();
    renderCart();
    updateCartCount();
    renderDeals();
    setupEventListeners();
    setupMobileMenu();
}

// ==========================================
// PRODUCT RENDERING
// ==========================================

function getFilteredProducts() {
    let filtered = [...products];
    
    // Search filter
    if (searchQuery) {
        const query = searchQuery.toLowerCase();
        filtered = filtered.filter(p => 
            p.name.toLowerCase().includes(query) || 
            p.category.toLowerCase().includes(query)
        );
    }
    
    // Category filter
    if (currentCategory !== 'all') {
        filtered = filtered.filter(p => p.category === currentCategory);
    }
    
    // Sort options
    switch (currentSort) {
        case 'price-low':
            filtered.sort((a, b) => a.currentPrice - b.currentPrice);
            break;
        case 'price-high':
            filtered.sort((a, b) => b.currentPrice - a.currentPrice);
            break;
        case 'rating':
            filtered.sort((a, b) => b.rating - a.rating);
            break;
        case 'newest':
            filtered.sort((a, b) => new Date(b.createdAt) - new Date(a.createdAt));
            break;
    }
    
    return filtered;
}

function renderStars(rating) {
    const fullStars = Math.floor(rating);
    const hasHalf = rating % 1 >= 0.5;
    let html = '';
    
    for (let i = 0; i < 5; i++) {
        if (i < fullStars) {
            html += '<i class="fas fa-star"></i>';
        } else if (i === fullStars && hasHalf) {
            html += '<i class="fas fa-star-half-alt"></i>';
        } else {
            html += '<i class="far fa-star"></i>';
        }
    }
    return html;
}

function getStockStatus(stock) {
    if (stock === 0) return { class: 'out-of-stock', text: 'Out of Stock' };
    if (stock <= 10) return { class: 'low-stock', text: 'Low Stock' };
    return { class: 'in-stock', text: 'In Stock' };
}

function renderProducts() {
    const grid = document.getElementById('productsGrid');
    const filtered = getFilteredProducts();
    
    if (filtered.length === 0) {
        grid.innerHTML = `
            <div class="loading" style="grid-column: 1/-1;">
                <p>No products found. Try a different search or category.</p>
            </div>
        `;
        return;
    }
    
    grid.innerHTML = filtered.map(product => {
        const stockStatus = getStockStatus(product.stock);
        return `
            <div class="product-card" data-id="${product.id}">
                <div class="product-image-container">
                    <img src="${product.image}" alt="${product.name}" class="product-image" loading="lazy">
                    ${product.discount > 0 ? `<span class="discount-badge">-${product.discount}%</span>` : ''}
                    <span class="stock-badge ${stockStatus.class}">${stockStatus.text}</span>
                </div>
                <div class="product-info">
                    <div class="product-category">${product.category}</div>
                    <h3 class="product-name">${product.name}</h3>
                    <div class="product-rating">
                        <span class="stars">${renderStars(product.rating)}</span>
                        <span class="rating-count">${product.rating} (${product.ratingCount})</span>
                    </div>
                    <div class="product-price">
                        <span class="current-price">$${product.currentPrice.toFixed(2)}</span>
                        ${product.originalPrice > product.currentPrice ? `<span class="original-price">$${product.originalPrice.toFixed(2)}</span>` : ''}
                    </div>
                    <div class="product-actions">
                        <button class="btn btn-primary" onclick="addToCart('${product.id}')" ${product.stock === 0 ? 'disabled' : ''}>
                            <i class="fas fa-cart-plus"></i> Add to Cart
                        </button>
                        <button class="btn btn-secondary" onclick="openProductDetails('${product.id}')">
                            <i class="fas fa-eye"></i> Details
                        </button>
                    </div>
                </div>
            </div>
        `;
    }).join('');
}

function renderDeals() {
    const dealsGrid = document.getElementById('dealsGrid');
    const deals = products
        .filter(p => p.discount >= 10)
        .sort((a, b) => b.discount - a.discount)
        .slice(0, 4);
    
    dealsGrid.innerHTML = deals.map(product => {
        const stockStatus = getStockStatus(product.stock);
        return `
            <div class="product-card" data-id="${product.id}">
                <div class="product-image-container">
                    <img src="${product.image}" alt="${product.name}" class="product-image" loading="lazy">
                    <span class="discount-badge">-${product.discount}%</span>
                    <span class="stock-badge ${stockStatus.class}">${stockStatus.text}</span>
                </div>
                <div class="product-info">
                    <div class="product-category">${product.category}</div>
                    <h3 class="product-name">${product.name}</h3>
                    <div class="product-rating">
                        <span class="stars">${renderStars(product.rating)}</span>
                        <span class="rating-count">${product.rating} (${product.ratingCount})</span>
                    </div>
                    <div class="product-price">
                        <span class="current-price">$${product.currentPrice.toFixed(2)}</span>
                        <span class="original-price">$${product.originalPrice.toFixed(2)}</span>
                    </div>
                    <div class="product-actions">
                        <button class="btn btn-primary" onclick="addToCart('${product.id}')" ${product.stock === 0 ? 'disabled' : ''}>
                            <i class="fas fa-cart-plus"></i> Add to Cart
                        </button>
                        <button class="btn btn-secondary" onclick="openProductDetails('${product.id}')">
                            <i class="fas fa-eye"></i> Details
                        </button>
                    </div>
                </div>
            </div>
        `;
    }).join('');
}

// ==========================================
// CART FUNCTIONS
// ==========================================

function addToCart(productId) {
    const product = products.find(p => p.id === productId);
    if (!product || product.stock === 0) {
        showToast('Product is out of stock', 'error');
        return;
    }
    
    const existingItem = cart.find(item => item.id === productId);
    if (existingItem) {
        if (existingItem.quantity >= product.stock) {
            showToast('Maximum stock reached', 'warning');
            return;
        }
        existingItem.quantity++;
    } else {
        cart.push({
            id: productId,
            quantity: 1,
            addedAt: new Date().toISOString()
        });
    }
    
    saveToStorage('moa_cart', cart);
    renderCart();
    updateCartCount();
    showToast(`${product.name} added to cart!`, 'success');
    
    // Animate cart icon
    const cartBtn = document.querySelector('.cart-btn');
    cartBtn.style.transform = 'scale(1.2)';
    setTimeout(() => cartBtn.style.transform = 'scale(1)', 200);
}

function removeFromCart(productId) {
    cart = cart.filter(item => item.id !== productId);
    saveToStorage('moa_cart', cart);
    renderCart();
    updateCartCount();
    showToast('Item removed from cart', 'success');
}

function updateCartQuantity(productId, change) {
    const item = cart.find(item => item.id === productId);
    const product = products.find(p => p.id === productId);
    
    if (!item || !product) return;
    
    const newQty = item.quantity + change;
    if (newQty <= 0) {
        removeFromCart(productId);
        return;
    }
    if (newQty > product.stock) {
        showToast('Maximum stock reached', 'warning');
        return;
    }
    
    item.quantity = newQty;
    saveToStorage('moa_cart', cart);
    renderCart();
    updateCartCount();
}

function clearCart() {
    cart = [];
    saveToStorage('moa_cart', cart);
    renderCart();
    updateCartCount();
    showToast('Cart cleared', 'success');
}

function renderCart() {
    const cartItems = document.getElementById('cartItems');
    const subtotalEl = document.getElementById('cartSubtotal');
    const totalEl = document.getElementById('cartTotal');
    
    if (cart.length === 0) {
        cartItems.innerHTML = `
            <div class="cart-empty">
                <i class="fas fa-shopping-bag"></i>
                <p>Your cart is empty</p>
            </div>
        `;
        subtotalEl.textContent = '$0.00';
        totalEl.textContent = '$0.00';
        return;
    }
    
    let subtotal = 0;
    
    cartItems.innerHTML = cart.map(item => {
        const product = products.find(p => p.id === item.id);
        if (!product) return '';
        const itemTotal = product.currentPrice * item.quantity;
        subtotal += itemTotal;
        
        return `
            <div class="cart-item">
                <img src="${product.image}" alt="${product.name}" class="cart-item-image">
                <div class="cart-item-details">
                    <div class="cart-item-name">${product.name}</div>
                    <div class="cart-item-price">$${product.currentPrice.toFixed(2)}</div>
                    <div class="cart-item-quantity">
                        <button type="button" class="qty-btn" onclick="updateCartQuantity('${item.id}', -1)">-</button>
                        <span>${item.quantity}</span>
                        <button type="button" class="qty-btn" onclick="updateCartQuantity('${item.id}', 1)">+</button>
                    </div>
                </div>
                <button type="button" class="remove-item" onclick="removeFromCart('${item.id}')">
                    <i class="fas fa-trash"></i>
                </button>
            </div>
        `;
    }).join('');
    
    subtotalEl.textContent = `$${subtotal.toFixed(2)}`;
    totalEl.textContent = `$${subtotal.toFixed(2)}`;
}

function updateCartCount() {
    const count = cart.reduce((sum, item) => sum + item.quantity, 0);
    document.getElementById('cartCount').textContent = count;
}

// ==========================================
// PRODUCT DETAILS MODAL
// ==========================================

function openProductDetails(productId) {
    const product = products.find(p => p.id === productId);
    if (!product) return;
    
    const stockStatus = getStockStatus(product.stock);
    const content = document.getElementById('productDetailContent');
    
    content.innerHTML = `
        <div class="product-detail-image-container">
            <img src="${product.image}" alt="${product.name}" class="product-detail-image">
        </div>
        <div class="product-detail-info">
            <div class="product-category">${product.category}</div>
            <h2>${product.name}</h2>
            <div class="product-rating" style="margin-bottom: 1rem;">
                <span class="stars">${renderStars(product.rating)}</span>
                <span class="rating-count">${product.rating} (${product.ratingCount} ratings)</span>
            </div>
            <div class="detail-price">
                $${product.currentPrice.toFixed(2)}
                ${product.originalPrice > product.currentPrice ? `<span class="original">$${product.originalPrice.toFixed(2)}</span>` : ''}
            </div>
            <div class="detail-stock">
                <i class="fas fa-box"></i> ${stockStatus.text} (${product.stock} available)
            </div>
            <p class="description">${product.description}</p>
            
            <div class="quantity-selector">
                <label>Quantity:</label>
                <input type="number" id="detailQuantity" value="1" min="1" max="${product.stock}">
            </div>
            
            <button class="btn btn-primary" onclick="addToCartFromDetails('${product.id}')" ${product.stock === 0 ? 'disabled' : ''}>
                <i class="fas fa-cart-plus"></i> Add to Cart
            </button>
            
            <div class="customer-rating">
                <h4>Rate this product</h4>
                <div class="rating-stars-input" id="customerRatingStars">
                    <i class="far fa-star" data-value="1"></i>
                    <i class="far fa-star" data-value="2"></i>
                    <i class="far fa-star" data-value="3"></i>
                    <i class="far fa-star" data-value="4"></i>
                    <i class="far fa-star" data-value="5"></i>
                </div>
                <button type="button" class="btn btn-secondary" onclick="submitCustomerRating('${product.id}')">Submit Rating</button>
            </div>
        </div>
    `;
    
    setupStarRatingInteraction();
    openModal('productModal', 'productModalOverlay');
}

function setupStarRatingInteraction() {
    const stars = document.querySelectorAll('#customerRatingStars i');
    
    stars.forEach(star => {
        star.addEventListener('mouseenter', function() {
            const value = parseInt(this.dataset.value);
            highlightStars(value);
        });
        
        star.addEventListener('click', function() {
            const selected = parseInt(this.dataset.value);
            this.parentElement.dataset.selected = selected;
            highlightStars(selected);
        });
    });
    
    document.getElementById('customerRatingStars').addEventListener('mouseleave', function() {
        const selected = parseInt(this.dataset.selected) || 0;
        highlightStars(selected);
    });
    
    function highlightStars(count) {
        stars.forEach((star, index) => {
            if (index < count) {
                star.classList.remove('far');
                star.classList.add('fas', 'active');
            } else {
                star.classList.remove('fas', 'active');
                star.classList.add('far');
            }
        });
    }
}

function addToCartFromDetails(productId) {
    const qty = parseInt(document.getElementById('detailQuantity').value) || 1;
    const product = products.find(p => p.id === productId);
    
    if (!product || product.stock === 0) return;
    
    const existingItem = cart.find(item => item.id === productId);
    const currentQty = existingItem ? existingItem.quantity : 0;
    
    if (currentQty + qty > product.stock) {
        showToast('Not enough stock available', 'error');
        return;
    }
    
    if (existingItem) {
        existingItem.quantity += qty;
    } else {
        cart.push({
            id: productId,
            quantity: qty,
            addedAt: new Date().toISOString()
        });
    }
    
    saveToStorage('moa_cart', cart);
    renderCart();
    updateCartCount();
    showToast(`${qty} x ${product.name} added to cart!`, 'success');
    closeModal('productModal', 'productModalOverlay');
}

function submitCustomerRating(productId) {
    const starsContainer = document.getElementById('customerRatingStars');
    const rating = parseInt(starsContainer.dataset.selected) || 0;
    
    if (rating < 1 || rating > 5) {
        showToast('Please select a rating', 'warning');
        return;
    }
    
    const product = products.find(p => p.id === productId);
    if (!product) return;
    
    // Calculate new average rating
    const totalRating = product.rating * product.ratingCount;
    const newCount = product.ratingCount + 1;
    const newRating = (totalRating + rating) / newCount;
    
    product.rating = Math.round(newRating * 10) / 10;
    product.ratingCount = newCount;
    
    saveToStorage('moa_products', products);
    renderProducts();
    renderDeals();
    
    // Refresh the modal to show updated rating
    openProductDetails(productId);
    showToast('Thank you for your rating!', 'success');
}

// ==========================================
// ADMIN PANEL
// ==========================================

function openAdminPanel() {
    renderAdminStats();
    renderAdminTable();
    openModal('adminModal', 'adminModalOverlay');
}

function renderAdminStats() {
    const stats = document.getElementById('adminStats');
    const categories = [...new Set(products.map(p => p.category))];
    const totalStock = products.reduce((sum, p) => sum + p.stock, 0);
    const avgRating = products.length > 0 
        ? products.reduce((sum, p) => sum + p.rating, 0) / products.length 
        : 0;
    
    stats.innerHTML = `
        <div class="stat-card">
            <h4>Total Products</h4>
            <p>${products.length}</p>
        </div>
        <div class="stat-card">
            <h4>Categories</h4>
            <p>${categories.length}</p>
        </div>
        <div class="stat-card">
            <h4>Total Stock</h4>
            <p>${totalStock}</p>
        </div>
        <div class="stat-card">
            <h4>Avg Rating</h4>
            <p>${avgRating.toFixed(1)}</p>
        </div>
    `;
}

function renderAdminTable() {
    const tbody = document.getElementById('adminTableBody');
    tbody.innerHTML = products.map(product => `
        <tr>
            <td>
                <div style="display: flex; align-items: center; gap: 0.75rem;">
                    <img src="${product.image}" alt="${product.name}">
                    <span style="font-weight: 600;">${product.name}</span>
                </div>
            </td>
            <td>${product.category}</td>
            <td>$${product.currentPrice.toFixed(2)}</td>
            <td>${product.stock}</td>
            <td>${product.rating} (${product.ratingCount})</td>
            <td>
                <div class="admin-actions-cell">
                    <button type="button" class="btn btn-primary" onclick="editProduct('${product.id}')">
                        <i class="fas fa-edit"></i> Edit
                    </button>
                    <button type="button" class="btn btn-secondary" onclick="openPriceModal('${product.id}')">
                        <i class="fas fa-tag"></i> Price
                    </button>
                    <button type="button" class="btn btn-secondary" onclick="openRatingModal('${product.id}')">
                        <i class="fas fa-star"></i> Rating
                    </button>
                    <button type="button" class="btn btn-danger" onclick="deleteProduct('${product.id}')">
                        <i class="fas fa-trash"></i> Delete
                    </button>
                </div>
            </td>
        </tr>
    `).join('');
}

function openAddProductForm() {
    document.getElementById('productFormTitle').textContent = 'Add Product';
    document.getElementById('productForm').reset();
    document.getElementById('editProductId').value = '';
    openModal('productFormModal', 'productFormOverlay');
}

function editProduct(productId) {
    const product = products.find(p => p.id === productId);
    if (!product) return;
    
    document.getElementById('productFormTitle').textContent = 'Edit Product';
    document.getElementById('editProductId').value = product.id;
    document.getElementById('formProductName').value = product.name;
    document.getElementById('formProductCategory').value = product.category;
    document.getElementById('formProductDescription').value = product.description;
    document.getElementById('formProductImage').value = product.image;
    document.getElementById('formOriginalPrice').value = product.originalPrice;
    document.getElementById('formCurrentPrice').value = product.currentPrice;
    document.getElementById('formStockQuantity').value = product.stock;
    document.getElementById('formInitialRating').value = product.rating;
    document.getElementById('formRatingCount').value = product.ratingCount;
    
    openModal('productFormModal', 'productFormOverlay');
}

function saveProductForm(e) {
    e.preventDefault();
    
    const id = document.getElementById('editProductId').value;
    const productData = {
        name: document.getElementById('formProductName').value,
        category: document.getElementById('formProductCategory').value,
        description: document.getElementById('formProductDescription').value,
        image: document.getElementById('formProductImage').value,
        originalPrice: parseFloat(document.getElementById('formOriginalPrice').value),
        currentPrice: parseFloat(document.getElementById('formCurrentPrice').value),
        stock: parseInt(document.getElementById('formStockQuantity').value),
        rating: parseFloat(document.getElementById('formInitialRating').value),
        ratingCount: parseInt(document.getElementById('formRatingCount').value),
        discount: 0
    };
    
    // Calculate discount percentage
    if (productData.originalPrice > productData.currentPrice) {
        productData.discount = Math.round(
            ((productData.originalPrice - productData.currentPrice) / productData.originalPrice) * 100
        );
    }
    
    if (id) {
        // Edit existing product
        const index = products.findIndex(p => p.id === id);
        if (index !== -1) {
            products[index] = { ...products[index], ...productData };
            showToast('Product updated successfully!', 'success');
        }
    } else {
        // Add new product
        const newProduct = {
            id: Date.now().toString(),
            ...productData,
            createdAt: new Date().toISOString().split('T')[0]
        };
        products.push(newProduct);
        showToast('Product added successfully!', 'success');
    }
    
    saveToStorage('moa_products', products);
    renderProducts();
    renderDeals();
    renderAdminStats();
    renderAdminTable();
    closeModal('productFormModal', 'productFormOverlay');
}

function deleteProduct(productId) {
    if (!confirm('Are you sure you want to delete this product?')) return;
    
    products = products.filter(p => p.id !== productId);
    cart = cart.filter(item => item.id !== productId);
    
    saveToStorage('moa_products', products);
    saveToStorage('moa_cart', cart);
    
    renderProducts();
    renderDeals();
    renderCart();
    updateCartCount();
    renderAdminStats();
    renderAdminTable();
    showToast('Product deleted successfully!', 'success');
}

function openPriceModal(productId) {
    const product = products.find(p => p.id === productId);
    if (!product) return;
    
    document.getElementById('priceProductId').value = productId;
    document.getElementById('updateOriginalPrice').value = product.originalPrice;
    document.getElementById
